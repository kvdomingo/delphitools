apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "delphitools.fullname" . }}-test-connection"
  labels:
    {{- include "delphitools.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "delphitools.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
